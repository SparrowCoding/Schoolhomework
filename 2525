#include <iostream>

using namespace std;

int main()
{
    int H, M;
    int p;
    cin >> H >> M;
    cin >> p;

    M += p;

    if (M >= 60) {
        H += M / 60;
        M = M % 60;
    }

    if (H >= 24) {
        H = H % 24;
    }

    cout << H << " " << M;
    
    return 0;
}
